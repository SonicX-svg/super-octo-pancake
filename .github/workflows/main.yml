name: Docker Build and Push

on:
  push:
    branches:
      - main
      
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Login to GitHub
        uses: github/login@v1
      - name: Build and push image
        run: |
          docker build -t dockerfile .
          docker push ${{ s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 }}/dockerfile:latest
